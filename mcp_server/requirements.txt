# MCP Server dependencies
# All RCA business logic lives here: Splunk, pgvector, LLM, log pipeline, scratchpad.
# server.py is plain FastAPI (POST /tools/{tool_name}) — no MCP wire protocol needed.

# Web framework + ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# PostgreSQL + pgvector (sync driver — compatible with ThreadPoolExecutor)
psycopg2-binary>=2.9.9
pgvector>=0.2.5

# HTTP client (async, for Splunk REST API + Loki)
httpx>=0.27.0

# LM Studio OpenAI-compatible client
openai>=1.0.0

# Pydantic for internal validation
pydantic>=2.0
pydantic-settings>=2.0
