# API Server dependencies
# Thin FastAPI orchestrator — calls MCP server, serves frontend, streams SSE.

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.0
pydantic-settings>=2.0
python-multipart>=0.0.9

# psycopg2 for direct pgvector calls (vectordb stats, app registry)
psycopg2-binary>=2.9.9
pgvector>=0.2.5

# For error generation thread
requests>=2.31.0

# Jinja2 for RCA report HTML rendering
jinja2>=3.1.0

# LangGraph pipeline orchestration
langgraph>=0.2.0

# LangChain AI wrappers (ChatOpenAI, OpenAIEmbeddings, PromptTemplate)
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain>=0.3.0
